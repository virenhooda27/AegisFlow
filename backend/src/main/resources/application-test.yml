spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aegisflow
    username: aegisflow
    password: aegisflow

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

  data:
    redis:
      host: localhost
      port: 6379
